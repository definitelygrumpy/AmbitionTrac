/*
 * ==============================================================================
 *
 *   NOTE: This component ('PassionInput') is deprecated and no longer used in the
 *   application. The app now uses 'AmbitionInput.tsx'. This file is a
 *   placeholder and can be safely removed.
 *
 * ==============================================================================
 */
import React from 'react';

const DeprecatedPassionInput: React.FC = () => {
    return null; // This component is deprecated.
};

export default DeprecatedPassionInput;
